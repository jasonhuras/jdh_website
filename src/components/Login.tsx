import { Typography } from "@mui/material"

export default function Login() {
    return (
        <Typography variant="h1" sx={{ textAlign: 'right', paddingRight: '40px' }} >
            Login
        </Typography>
    )
}